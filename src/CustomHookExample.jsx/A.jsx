import React,{useState} from 'react'

const A = () => {
    const[count,setCount] = useState(0)
    return(
        <div>
            
            <button>Count</button>
        </div>
    )
}

export default A;